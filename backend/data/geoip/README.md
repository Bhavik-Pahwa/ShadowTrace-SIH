# GeoIP Databases

Place the offline MaxMind databases required by the PRD in this directory:

- `GeoLite2-City.mmdb`
- `GeoLite2-ASN.mmdb`

These files are not generated by the application. The backend validates their
presence in `scripts/verify_offline.py` and uses them through `geoip2` without
remote lookups.
